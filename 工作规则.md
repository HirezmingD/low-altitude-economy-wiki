# 工作规则 — 低空经济研究知识库规则书

> 这是本知识库规则文件。
> 它定义了目录结构、命名规范、页面格式、学习工作流、查询维护规则。LLM 必须严格遵循此文件。

---

## 1. 三层架构

```
原始素材/       ← 原始素材（只读，LLM 永不修改）
知识库/         ← LLM 维护的 Markdown 页面（LLM 全权负责）
工作规则.md     ← 本文件，定义规则
```

---

## 2. 目录结构

```
.
├── 工作规则.md                 ← 本规则文件
├── 研究意图.md                 ← 方向意图（目标、关键问题、研究范围）
├── 说明文档.md                 ← 知识库概览
├── 目录.md                     ← 内容目录（按类别列出所有页面）
├── 日志.md                     ← 活动日志（追加写入，最新在最顶）
├── 概览.md                     ← 高层综述（动态更新，反映知识库全貌）
│
├── _待审核/                    ← 分析报告暂存区（步骤 1 输出，等待审核）
├── _scripts/                   ← 工具脚本（wf-gate.ps1、log-lint.ps1、link-lint.py 等）
│
├── 原始素材/                   ← 原始素材（只读！）
│   ├── 论文/                   ← 学术论文（期刊、会议、学位论文）
│   ├── 报告/                   ← 政府报告、行业报告、规划文件
│   ├── 文章/                   ← 网页文章、博客、新闻报道
│   ├── 政策文件/               ← 政策文件、法律法规
│   ├── 标准文件/               ← 标准文件（地方/行业/团体标准）
│   ├── 全球比较/               ← 国际比较材料（各国低空政策、案例）
│   │   ├── 美国/
│   │   ├── 欧盟/
│   │   ├── 日本/
│   │   ├── 阿联酋/
│   │   └── 新加坡/
│   ├── 附件/                   ← 图片、表格截图、数据文件
│   └── 项目文件/               ← 项目内部文件（申报书、框架、会议纪要）
│
├── 知识库/                     ← LLM 维护的知识库页面
│   ├── 概念/                   ← 概念页（术语、理论、方法框架）
│   │   ├── 空域治理/            ← 权属、法律、制度、数字孪生
│   │   ├── 容量评估/            ← 承载力、动态容量、饱和度、预测
│   │   ├── 智能调度/            ← 多智能体、冲突解脱、MARL、决策辅助
│   │   └── 低空经济/            ← 产业生态、商业模式、应用场景
│   │
│   ├── 实体/                   ← 实体页
│   │   ├── 国家地区/            ← 美国、欧盟、日本、阿联酋、新加坡等
│   │   ├── 示范区域/            ← 南浔、余杭、上城等示范区
│   │   ├── 平台系统/            ← 仿真验证平台、数字孪生底座等
│   │   └── 机构组织/            ← ICAO、FAA、EASA 等
│   │
│   ├── 摘要/                   ← 素材摘要（每篇素材一篇）
│   │   ├── 全球比较/            ← 国际比较研究素材
│   │   ├── 空域治理/            ← 权属、法律、数字孪生素材
│   │   ├── 容量评估/            ← 承载力评价、态势预测素材
│   │   ├── 智能调度/            ← MARL、冲突解脱、调度策略素材
│   │   ├── 产业转化/            ← 集成验证、商业模式素材
│   │   └── 政策标准/            ← 政策法规、标准规范素材
│   │
│   ├── 技术/                   ← 技术页（具体方法、算法、模型）
│   │   ├── 容量建模/            ← 容量评估模型、指标体系
│   │   ├── 机器学习/            ← RF、ST-GNN、MARL 等
│   │   ├── 时空分析/            ← GIS、遥感、空间统计
│   │   ├── 多智能体/            ← 分布式算法、强化学习
│   │   └── 数字孪生/            ← 数字底座、仿真平台
│   │
│   ├── 政策/                   ← 政策页（分类整理政策文件）
│   │   ├── 中国/                ← 中央及地方低空政策
│   │   └── 国际/                ← 各国低空政策法规
│   │
│   ├── 综合分析/               ← 综合页（对比分析、跨主题综合）
│   │
│   ├── 文献综述/               ← 文献综述（结构化知识地图）
│   │   └── 文献综述.md
│   │
│   ├── 标准/                   ← 标准整理页
│   │
│   └── 图片/                   ← 技术插图（框架图、流程图等）
│
└── 模板/                       ← 页面模板
    ├── 分析报告模板.md          ← 分析报告模板
    ├── 论文摘要模板.md          ← 论文摘要模板
    ├── 概念模板.md              ← 概念页模板
    └── 实体模板.md              ← 实体页模板
```

---

## 3. 命名规范

- **知识库页面文件名**：使用中文（如 `低空空域动态容量评估.md`），直击概念本身
- 文件名中避免使用 Windows 禁用的字符（`\` `/` `:` `*` `?` `"` `<` `>` `|`）
- **原始素材**：保留原始文件名，避免重命名
- **日期格式**：`YYYY-MM-DD`
- **分析报告**：`_待审核/分析-YYYY-MM-DD-概念简述.md`

---

## 4. 页面格式

### 4.1 通用 frontmatter（YAML）

```yaml
---
标题: 低空空域动态容量评估模型
类型: 概念                # 概念 | 实体 | 摘要 | 技术 | 综合分析 | 政策
标签: [低空空域, 容量评估, 承载力, 动态模型]
创建日期: 2026-06-29
更新日期: 2026-06-29
来源: [原始素材/论文/某论文.pdf]    # 引用的原始素材路径
别名: [dynamic capacity assessment, 空域容量]
---
```

### 4.2 内容规范

- 使用 **Markdown 链接** 引用知识库内其他页面：`[动态容量评估](知识库/概念/容量评估/空域复杂度.md)`
- 引用原始素材：`(来源: xxx, 2024)` 或 `` `原始素材/论文/某论文.pdf` ``
- 每条主张/数据标注来源
- 对比 / 争议点使用表格或列表清晰呈现
- **国际比较内容**需明确标注国家/地区和来源时间

### 4.3 页面类型与内容模板

| 类型 | 存放位置 | 内容要求 |
|:---|:---|:---|
| **概念** | `知识库/概念/` | 术语定义、理论框架、核心方法，含该概念在不同文献中的表述差异 |
| **实体** | `知识库/实体/` | 国家/地区/区域的低空治理概况、关键数据、演进时间线 |
| **摘要** | `知识库/摘要/` | 单篇素材的结构化摘要（背景-方法-发现-与知识库关联） |
| **技术** | `知识库/技术/` | 具体算法/模型的技术细节、输入输出、适用条件、局限性 |
| **综合分析** | `知识库/综合分析/` | 跨素材的综合对比、趋势分析、研究缺口识别 |
| **政策** | `知识库/政策/` | 政策文件的关键条款、适用范围、与其他政策的关联 |

---

## 5. 学习工作流（三步强制流程）

> **⚡ 这是硬性规则，不可绕过。**
> 学习流程拆分为**三次单独的 LLM 调用**，中间必须有人工审核。
> LLM 在任何情况下都不得在一步调用中完成两个阶段的工作。
> 不得使用子代理（sessions_spawn）绕过两步分离的限制。

### 强制机制总览

```
主人说"学习这个素材" → 第一步：分析（只写 _待审核/，绝不碰 知识库/）
                             ↓
主人审核分析报告 → 确认或要求调整
                             ↓
主人说"继续" → 第二步：生成（先跑 wf-gate.ps1 门禁检查，再创建知识库页面）
                             ↓
主人说"检查" → 第三步：验证（运行 log-lint.ps1 日志检查 + link-lint.py 链接检查）
```

**物理门禁**：每次进入生成步骤前，LLM **必须**运行 `_scripts/wf-gate.ps1`。如果该脚本返回 exit code 1（拦截），LLM **不得创建或修改任何知识库页面**。

---

### 第一步：分析（只写 _待审核/，绝不碰 知识库/）

**触发方式**：主人说出"学习这个素材"

**执行步骤**：

1. 读取 `研究意图.md`，回顾核心目标与关键问题
2. 读取原始素材中的源文件
3. 按照 `模板/分析报告模板.md` 模板生成结构化分析报告
4. 将报告写入 `_待审核/分析-YYYY-MM-DD-slug.md`
5. 向主人展示分析报告要点，标注**需人工判断的审核项**
6. **列出后续需要创建/更新的所有页面清单**

**输出**：`_待审核/分析-YYYY-MM-DD-slug.md`

**此时 LLM 不会写入任何知识库文件。**

---

### 第二步：生成（先跑门禁检查，再创建页面）

**触发方式**：主人说"继续"或"生成"

**前置检查（不可跳过）**：

```powershell
powershell -NoProfile -File _scripts/wf-gate.ps1
# 如果 exit code != 0，停止！
```

**执行步骤**：

1. 读取 `_待审核/` 中对应的分析报告
2. 按分析报告中的结构建议逐一执行，必须覆盖以下所有适用项：

   a. 创建/更新摘要页 → `知识库/摘要/`（按主题放入对应子文件夹）
   b. 创建/更新概念页 → `知识库/概念/`（按主题放入对应子文件夹）
   c. 创建/更新实体页 → `知识库/实体/`（按类型放入对应子文件夹）
   d. 创建/更新技术页 → `知识库/技术/`（按方法论放入对应子文件夹）
   e. 如果涉及政策/标准 → 更新 `知识库/政策/` 或 `知识库/标准/`

3. 如果分析报告建议生成综合对比页 → 写入 `知识库/综合分析/`
4. **更新文献综述** → `知识库/文献综述/文献综述.md`（每篇新素材都必须增加对应条目，标注与已有文献的张力/互补/演替关系）
5. 更新 `目录.md`：添加所有新建/更新的页面条目
6. 更新 `概览.md`：如果整体知识结构有显著变化
7. **更新 `研究意图.md`**：如果素材对核心假设或研究范围有影响
8. 追加 `日志.md`：倒序插入（新条目在最顶部）
9. 标记分析报告为 `✅ 已处理`
10. **清理 `_待审核/` 目录**：删除所有分析报告文件

---

### 第三步：验证（运行日志检查和链接检查）

**触发方式**：主人说"检查"

```powershell
powershell -NoProfile -File _scripts/log-lint.ps1
```

确认原始素材/ 中所有文件在 日志.md 中都有对应记录。

---

## 6. 日志格式（追加写入）

### 6.1 学习日志

```markdown
## [2026-06-29] 学习 | 论文/报告标题（作者，年份）
- 源文件：`原始素材/子目录/文件名.pdf`
- 分析报告：`_待审核/分析-2026-06-29-slug.md`
- **新建摘要页**：`知识库/摘要/主题子目录/页面.md`（核心发现概述）
- **新建概念页**：`知识库/概念/子目录/概念.md`（定义了 X 概念）
- **更新概念页**：`知识库/概念/子目录/已有概念.md`（补充 Y 视角）
- **新建实体页**：`知识库/实体/子目录/实体.md`
- **新建技术页**：`知识库/技术/子目录/技术.md`
- **更新文献综述**：`知识库/文献综述/文献综述.md`（增补到 Z 模块）
- 更新 `目录.md`（新增 N 个页面条目）
- 更新 `概览.md`（素材数 +1）
- 分析报告：✅ 已处理
```

### 6.2 其他日志类型

```markdown
## [2026-06-29] 查询 | 用户问题简述
- 生成的页面：`知识库/综合分析/xxx.md`
- 关键发现：简要说明

## [2026-06-29] 检查 | 健康检查
- 发现的问题：xxx
- 修复：xxx
```

---

## 7. 查询工作流

当主人提问时：
1. 先阅读 `研究意图.md`，理解当前的研究方向和关键问题
2. 搜索 `目录.md` 找到相关页面
3. 读取相关页面内容
4. 合成答案，并**将高质量答案回写入知识库** 作为新的综合分析页（如果值得保留）
5. 引用具体页面路径作为来源

---

## 8. 检查

### 8a. 日志检查 — 防止日志遗漏

**脚本位置**：`_scripts/log-lint.ps1`

**触发时机**：
1. 每次学习完成后自动运行
2. 每天首次呼叫前运行一次
3. 主人可随时要求运行

**使用方式**：
```powershell
powershell -NoProfile -File _scripts/log-lint.ps1
```

**工作原理**：
1. 扫描 原始素材/ 下所有文件
2. 解析 日志.md 中所有 `` ` 引用的文件路径
3. 报告无对应记录的原始素材文件
4. 返回 exit code 0（通过）或 1（发现问题）

### 8b. 常规知识库健康检查

- 跨页面矛盾？（特别关注：同一国家/地区的低空政策在不同来源中的矛盾表述）
- 过时信息需要更新？
- 孤立页面（没有入链）？
- 有重要概念缺少独立页面？
- 缺失的交叉引用？
- 国际比较数据是否有时效性问题？

---



### 8c. 链接检查 — 防止断链

**脚本位置**：`_scripts/link-lint.py`

**触发时机**：
1. 每次学习完成后自动运行
2. 批量合并或重组知识库后运行
3. 每天首次呼叫前运行一次

**使用方式**：
```powershell
python _scripts/link-lint.py --quiet  # 检查报告
python _scripts/link-lint.py --fix               # 自动修复（谨慎）
python _scripts/link-lint.py --quiet             # 仅返回 exit code
```

**工作原理**：
1. 构建全知识库文件名→路径索引
2. 扫描 `知识库/` 下所有 .md 页面中的 `[text](target)` 链接
3. 解析相对路径，验证目标文件是否存在
4. 自动跳过外部链接（http/https）、锚点、data URI、mailto
5. `-Fix` 模式下自动尝试按文件名匹配修复断链
6. 返回 exit code 0（通过）或 1（发现断链）

**链接编写规范**：
- 知识库内部链接使用相对路径（如 `[概念](知识库/概念/空域治理/xxx.md)`）
- 大写 `知识库/` 开头，文件扩展名 `.md` 不可省略
- 引用其他类别页面时使用完整路径（如 `[摘要页](知识库/摘要/容量评估/xxx.md)`）
- 同一目录内可用相对路径，但推荐使用绝对路径避免目录重组后断链
- 原始素材引用使用完整路径（如 `` `原始素材/论文/xxx.pdf` ``）不含链接语法
- 新增页面后必须同步更新 `目录.md` 并确保链接指向正确路径
## 9. 编码规范（关键！）

### 9.1 唯一编码标准

- **所有 `.md` 文件必须为 UTF-8 with BOM（EF BB BF）**
- 严禁 GBK、GB2312、ANSI 等其他编码

### 9.2 写入文件时的编码保障

- **优先使用本会话的 `write` 工具写入文件**——已验证能正确输出 UTF-8 with BOM
- 子代理写入文件存在编码风险，其产出必须在主会话中验证
- 禁止使用 PowerShell 的 `Out-File`、`Set-Content`、`>` 重定向写入含中文的文件
- 如必须用脚本批量操作，使用 Python（`encoding='utf-8'` 显式指定）：
  ```python
  with open(path, 'w', encoding='utf-8') as f:
      f.write('\ufeff' + content)
  ```

### 9.3 写入后即时验证

创建/修改文件后立即用 `read` 工具抽查 frontmatter 的中文是否可读：
```
read path limit=3
```

---

## 10. 文献综述维护规则

### 10.1 综述定位

`知识库/文献综述/文献综述.md` 是本知识库的**结构化知识地图**，按主题模块组织，标注不同文献间的**张力、演替、互补、延伸、奠基**关系。**不是论文摘要的堆砌——是叙述性综述，行文流畅连贯，三段式拼接是禁止的。**

### 10.2 综述模块结构

综述包含以下七大模块（不可自行增删或重组模块编号）：

| 模块 | 对应课题方向 | 核心问题 | 子结构 |
|:---|:---|:---|:---|
| 一、低空空域治理与权属 | 子课题1 | 空域权属的法律框架、各国治理模式比较 | 文献谱系表→文献关系→法律政策底座 |
| 二、全球低空治理模式比较 | 子课题1 | 各国政策、产业生态、战略路径差异 | 治理模式分类→国际制度对比→深度解析代表性国家 |
| **三、空域容量评估** ☆ | **子课题3** | **容量模型、指标体系、态势预测、动态环境因子** | **理论基础→结构-容量→度量方法谱系→环境因子→方法总表→文献关系→研究空白** |
| 四、多智能体调度与仿真 | 子课题4 | MARL算法、冲突解脱、调度策略、数字孪生 | 分两个子节 |
| 五、场景应用与产业化 | 子课题5 | 物流/载人/应急场景、商业模式 | 场景谱系表→产业化结论 |
| 六、实证案例谱系 | 子课题1-5 | 国内外案例的核心验证点 | 统一表格，每案例含关联页面链接 |
| 七、研究空白总览 | 子课题1-5 | 跨模块研究缺口 | 分理论/方法/环境因子/实证四层 |

> ☆ 模块三为核心方向，该模块的更新具有最高优先级，不可省略子结构中的任何一环。

### 10.3 链接编写规范（硬约束）

- **每条文献首次出现时必须以 Markdown 链接指向其对应的知识库页面**（摘要页优先，其次概念页/实体页/技术页），格式：`[作者 (年份)](知识库/路径/页面.md)`
- 同一文献在文献关系段落再次出现时仍应保留链接
- 禁止仅以纯文本方式标注文献——如 `张三等 (2026)` 必须写成 `[张三等 (2026)](知识库/摘要/... 某页.md)`
- 文献关系箭头 `↔` 表示互补，`→` 表示演替或延伸
- 新增文献后必须运行 `python _scripts/link-lint.py` 验证链接有效

### 10.4 更新要求

每次学习新素材后：

**必须执行**：
- 在对应模块的文献谱系表中追加新文献行（含关联页面链接）
- 在文献关系段落更新关系标注（互补/演替/张力/延伸/奠基——选择最准确的一种关系）
- 如果新文献包含实证数据，同步更新模块「六、实证案例谱系」表格
- 如果新文献揭示了新的研究缺口，同步更新模块「七、研究空白总览」

**按需执行**：
- 如果新文献提出新方法论，更新模块三的「研究方法谱系总览」表格
- 如果新文献改变了某个模块的核心认知，重写该模块的引导段落
- 日期更新至 `更新日期` frontmatter 字段

**禁止**：
- ❌ 在综述末尾追加独立段落而不融入模块结构
- ❌ 三人分别维护各自的模块导致结构分裂（综述是单一文档，所有更新由 LLM 统一编排）
- ❌ 文献条目无链接
- ❌ 模块编号或标题名自行变更

---

## 11. 禁忌

- ❌ 不要修改 `原始素材/` 下的文件
- ❌ 不要在文件名中使用空格或中文标点
- ❌ 不要留下占位符或 "TODO" 不处理
- ❌ 不要使用 PowerShell 的 `>` 重定向或 `Out-File` 写入含中文的文件（默认编码 GBK）
- ❌ 不要让子代理直接读写中文文件而不在主会话中验证
- ❌ 不跳过三步工作流
- ❌ 不在 `_待审核/` 为空时写入任何知识库页面
- ✅ 有疑问就问主人

---

## 12. 项目特殊规则

### 12.1 课题核心方向

- 本知识库重点关注**子课题3「动态复杂环境下低空空域资源承载力评价与量化技术」**，所涉素材和页面在 日志.md 和 目录.md 中用 `【核心方向】` 标注

### 12.2 国际比较素材处理

- 国际比较素材（美国、欧盟、日本、阿联酋、新加坡）必须：
  - 在摘要中标注国家/地区标签
  - 在实体中创建或更新对应国家/地区页面
  - 在文献综述的全球比较模块中更新
- 比较分析应明确治理模式分类（市场主导型 / 规则引领型 / 政企协同型 / 制度特区型 / 城市密集型）

### 12.3 技术栈关键词

项目涉及的核心技术方法（用于标签和搜索）：

| 类别 | 关键词 |
|:---|:---|
| 容量评估 | 随机森林、MCDA、多因素耦合、折减系数、静态容量基准 |
| 态势预测 | ST-GNN、时空图神经网络、短临预测、交通流 |
| 智能调度 | MARL、多智能体深度强化学习、冲突探测与解脱、分布式 |
| 数字底座 | 三维时空编码、地理网格、区块链确权、数字孪生 |
| 场景验证 | 虚实融合、全流程仿真、实飞验证、集成测试 |

---

*本规则文件将随项目推进和知识库使用经验持续迭代。每次重大修订需在 日志.md 中记录。*
